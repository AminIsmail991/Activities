// 1. Write a function to find the sum of 2 numbers.

// Algorithm 
// Step 1: Declare variable number1 as int
// Step 2: Declare variable number2 as int
// Step 3: Declare variable sum as int
// Step 4: Calculation process (sum = number1 + number2)
// Step 5: Print sum 

function add(){
    const number1 = 12;
    const number2 = 15;

    // adding 2 numbers
    const sum = number1 + number2;

    // display sum
    console.log(`${number1} + ${number2} = ${sum}`);
} 

// call function
add();


////////////////////////////////////////////////////////////

const adding = sum(10,20);

function sum(a,b){
    return a + b ;

}